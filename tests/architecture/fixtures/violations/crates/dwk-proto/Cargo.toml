# FIXTURE: the shared wire-contract crate, broken in its manifest two ways
# (and in src/lib.rs a third, TX002):
#   * it links an in-tree crate, which makes it a bridge (RS009);
#   * it links a parser outside the TCB allowlist as a NORMAL dependency (RS004).
# The dev-dependency below is not linked and must not be reported; the
# checker's unit tests assert that separately.
[package]
name = "dwk-proto"
version = "0.0.0"

[dependencies]
dwk-common = { path = "../dwk-common" }
serde_json = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
