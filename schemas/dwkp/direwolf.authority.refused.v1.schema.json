{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:direwolf:schema:dwkp:direwolf.authority.refused:1",
  "title": "direwolf.authority.refused v1",
  "description": "The request was well-formed; the authority's state refused it.",
  "type": "object",
  "properties": {
    "v": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1,
      "x-direwolf-type": "Version"
    },
    "id": {
      "type": "string",
      "minLength": 30,
      "maxLength": 30,
      "pattern": "^msg_[0-7][0-9A-HJKMNP-TV-Z]{25}$",
      "x-direwolf-type": "MessageId",
      "x-direwolf-format": "uuid7-id",
      "x-direwolf-id-prefix": "msg"
    },
    "type": {
      "const": "response",
      "x-direwolf-type": "MessageType"
    },
    "schema": {
      "const": "direwolf.authority.refused",
      "x-direwolf-type": "SchemaName"
    },
    "schema_version": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1,
      "x-direwolf-type": "Version"
    },
    "ts": {
      "type": "string",
      "maxLength": 24,
      "x-direwolf-type": "Timestamp",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
      "x-direwolf-format": "rfc3339-utc-millis"
    },
    "correlation_id": {
      "type": "string",
      "maxLength": 35,
      "pattern": "^[a-z]{2,8}_[0-7][0-9A-HJKMNP-TV-Z]{25}$",
      "x-direwolf-type": "AnyId",
      "x-direwolf-format": "uuid7-id"
    },
    "causation_id": {
      "type": "string",
      "maxLength": 35,
      "pattern": "^[a-z]{2,8}_[0-7][0-9A-HJKMNP-TV-Z]{25}$",
      "x-direwolf-type": "AnyId",
      "x-direwolf-format": "uuid7-id"
    },
    "payload": {
      "$ref": "#/$defs/AuthorityRefusal"
    }
  },
  "required": ["v", "id", "type", "schema", "schema_version", "ts", "causation_id", "payload"],
  "additionalProperties": false,
  "not": {
    "anyOf": [
      {
        "required": ["session_id"]
      },
      {
        "required": ["run_id"]
      },
      {
        "required": ["epoch"]
      },
      {
        "required": ["idempotency_key"]
      }
    ]
  },
  "x-direwolf-family": "dwkp",
  "x-direwolf-message-type": "response",
  "x-direwolf-schema": "direwolf.authority.refused",
  "x-direwolf-schema-versions": {
    "min": 1,
    "max": 1
  },
  "x-direwolf-envelope-versions": {
    "min": 1,
    "max": 1
  },
  "x-direwolf-envelope": {
    "correlation_id": "optional",
    "causation_id": "required",
    "session_id": "forbidden",
    "run_id": "forbidden",
    "epoch": "forbidden",
    "idempotency_key": "forbidden"
  },
  "x-direwolf-id-prefix": "msg",
  "x-direwolf-unknown-fields": "reject",
  "x-direwolf-payload": "AuthorityRefusal",
  "$defs": {
    "AuthorityRefusal": {
      "type": "object",
      "description": "The authority declined to act on a well-formed request.\n\n**Three different things a caller must be able to tell apart**, and this\nis the middle one:\n\n* `direwolf.protocol.error` — the bytes did not form a valid message.\nNothing was evaluated because nothing arrived. The remedy is to fix\nthe message.\n* **this** — the message was valid and the authority's own state does\nnot permit the operation to be attempted. No policy ran, no capability\nwas consulted. The remedy is named by `reason` and is usually to\nre-acquire a lease, admit a run, or stop retrying.\n* `direwolf.authority.effective` carrying a `DENY` decision — both gates\nof ADR-0006 ran against real state and refused. The remedy is to ask\nfor less, or to change policy.\n\nMerging any two of those to save a message type would leave a caller\nunable to choose between three different remedies, and the failure mode\nof guessing is a retry loop against a refusal that will never change.\n\n**Two fields, and no third.** There is no `detail` string, no `hint`, no\nmap: `ProtocolErrorPayload` carries a detail because a malformed message\nneeds a human to repair *a message*, whereas a refusal needs the caller\nto take one of five known actions. Every byte beyond the reason is a\nbyte the authority tells a caller about state the caller could not\notherwise see — which is how a refusal becomes an oracle. `STALE_EPOCH`\nin particular does **not** carry the kernel's current epoch: handing a\nfenced runtime the one value it needs to un-fence itself would undo the\nfencing.",
      "properties": {
        "operation": {
          "type": "string",
          "enum": ["ACQUIRE_LEASE", "RELEASE_LEASE", "HEARTBEAT", "ADMIT_RUN", "RELEASE_RUN", "QUERY_AUTHORITY"],
          "x-direwolf-type": "RefusedOperation",
          "description": "Which operation was refused. Redundant with `causation_id` for a\ncaller that still has the request; not redundant for an audit\nrecord, and it is what closes the pairing below."
        },
        "reason": {
          "type": "string",
          "enum": ["STALE_EPOCH", "LEASE_HELD", "IDEMPOTENCY_CONFLICT", "UNKNOWN_AGENT_PROFILE", "UNKNOWN_RUN"],
          "x-direwolf-type": "RefusalReason",
          "description": "Why. Closed, and narrower still per operation."
        }
      },
      "required": ["operation", "reason"],
      "additionalProperties": false,
      "x-direwolf-unknown-fields": "reject",
      "x-direwolf-check": {
        "kind": "paired",
        "left": "operation",
        "right": "reason",
        "allowed": {
          "ACQUIRE_LEASE": ["LEASE_HELD"],
          "RELEASE_LEASE": ["STALE_EPOCH"],
          "HEARTBEAT": ["STALE_EPOCH"],
          "ADMIT_RUN": ["STALE_EPOCH", "IDEMPOTENCY_CONFLICT", "UNKNOWN_AGENT_PROFILE"],
          "RELEASE_RUN": ["STALE_EPOCH"],
          "QUERY_AUTHORITY": ["STALE_EPOCH", "UNKNOWN_RUN"]
        }
      }
    }
  }
}
