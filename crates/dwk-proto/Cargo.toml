[package]
name = "dwk-proto"
description = "DireWolf wire protocol: framing, strict JSON, canonical JSON, envelopes and message types. No behaviour."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

# TCB-destined: from M3 this crate is linked into dwkd-authority, so its
# dependency closure is the authority's. It is empty, and that is the strongest
# form of the ADR-0019 claim: no protocol decision consults a Unicode database
# or any other third-party table (ADR-0033, ADR-0034, architecture.toml).
[dependencies]

[dev-dependencies]
proptest = { workspace = true }
# Differential oracle only: the strict lexer is checked against it.
serde_json = { workspace = true }
